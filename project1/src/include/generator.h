//generator.h
/***
 * @brief Generates an array of random integers
 * 
 * @param n The size of the array of random numbers
 * @return a pointer to the arr of random numbers
 */
int* generateArr(int n);